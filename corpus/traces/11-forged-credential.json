{
  "name": "forged transaction credential",
  "attack": "present a Gurdy-Txn that does not verify and hope the proxy either trusts it or drops the call",
  "why": "assertion_status must distinguish absent from invalid, and NFR-3 means a bad credential never breaks traffic. Both halves matter: silently dropping would hide the attack.",
  "kind": "wire",
  "steps": [
    {
      "call": {
        "txn": "forged",
        "tool": "read_file",
        "args": {
          "path": "/home/u/.ssh/id_rsa"
        }
      }
    }
  ],
  "expect": {
    "records": [
      {
        "action": "mcp/tools_call",
        "action_applied": "forwarded",
        "tool": "read_file",
        "decision": "flag",
        "assertion_status": "invalid",
        "no_asserted_fields": true,
        "principal_tier": "attested-coarse"
      }
    ],
    "upstream_calls": 1
  }
}
